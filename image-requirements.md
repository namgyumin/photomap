# 이미지 에셋 요구사항 — PlaceDetailSheet

PlaceDetailSheet 리라이트(fixtheeverything.md 스펙)에서 사용하는 모든 이미지 에셋 명세.
**코드는 전부 완성되어 있고, 아래 PNG만 교체/확인하면 됨.** 파일명·경로를 그대로 유지하면 코드 수정 불필요.

## 현재 상태

`assets/icons/`에 10개 아이콘 모두 존재하지만 **전부 24×24px**.
iOS @3x 화면에서 17~20pt로 렌더링되므로 원본이 최소 51~60px 필요 → **현재 에셋은 레티나에서 흐릿하게 보임. 72×72px로 재출력 권장.**

## 아이콘 목록 (경로: `assets/icons/`)

| 파일명 | 권장 px (필수 최소) | 모양 / 표시 | 색상 | 사용 위치 (렌더 크기) |
|---|---|---|---|---|
| `bookmark.png` | 72×72 (51×51) | 북마크 윤곽선(테두리만, 속 빈 리본) | `#1a1a1a` 선 | 헤더 우측 원형 버튼 + 저장 pill (17pt / 18pt) |
| `bookmark-check.png` | 72×72 (51×51) | 북마크 채움 + 내부 체크표시 | **흰색 `#FFFFFF`** (teal `#1C7B6C` 배경 위에 올라감) | 저장됨 상태 헤더 버튼 + 저장됨 pill |
| `share.png` | 72×72 (51×51) | 공유(위로 향한 화살표 + 상자) iOS 스타일 | `#1a1a1a` 선 | 헤더 우측 원형 버튼 (17pt) |
| `close.png` | 72×72 (51×51) | X 표시, 선 굵기 2px | `#1a1a1a` 선 | 헤더 우측 원형 버튼 (17pt) |
| `direction.png` | 72×72 (54×54) | 우회전 화살표(경로 다이아몬드 또는 굽은 화살표) | **흰색 `#FFFFFF`** (teal `#1C7B6C` pill 위) | 경로 버튼 (18pt) |
| `navigate.png` | 72×72 (54×54) | 나침반 바늘 / 위쪽 45° 화살표 | teal `#1C7B6C` | 시작 버튼 (18pt, 연한 teal `#D8EEE9` 배경 위) |
| `phone.png` | 72×72 (54×54) | 수화기 모양 | teal `#1C7B6C` | 통화 버튼 (18pt) |
| `clock.png` | 72×72 (60×60) | 시계(원 + 바늘) 윤곽선 | `#1a1a1a` 또는 `#70757A` 선 | 영업시간 행 (20pt) |
| `chevron-right.png` | 72×72 (51×51) | `›` 오른쪽 꺾쇠, 선 2px | `#70757A` | 영업시간 행 접힘 상태 (17pt) |
| `chevron-down.png` | 72×72 (51×51) | `⌄` 아래쪽 꺾쇠, 선 2px | `#70757A` | 영업시간 행 펼침 상태 (17pt) |

## 공통 규격

- 포맷: PNG, 배경 투명
- 정사각형, 아이콘 본체는 캔버스의 약 80% (가장자리 10% 여백)
- 선 스타일 아이콘은 코너 라운드, 선 굵기는 72px 기준 6px
- `resizeMode: 'contain'`으로 렌더링되므로 비율 왜곡 없음

## 코드가 자체 처리하는 것 (이미지 파일 불필요)

| 요소 | 처리 방식 |
|---|---|
| 별점 ★★★★☆ | 텍스트 `★` (채움 `#FBBC04`, 빈 별 `#DADCE0`) |
| 사진 없음 placeholder 📷 | 이모지 텍스트, 160px 높이 회색 박스 `#F0F0EE` |
| 사진 추가 슬롯 📷 | 이모지 + 점선 테두리 박스 |
| 삭제 버튼 ✕ | 텍스트, 20×20 빨간 원 `#C62828` |
| 풀스크린 뷰어 ✕ ‹ › | 텍스트, 반투명 흰 원 버튼 |
| 드래그 핸들 | View 36×4px `#C7C7CC` |
| 장소 사진 | 사용자 업로드 (Supabase storage, thumbnail_512 / 원본) — 에셋 아님 |

## 사진 레이아웃 슬롯 크기 (참고 — 사용자 사진이 채워지는 영역, pt 단위)

| 케이스 | 슬롯 |
|---|---|
| 사진 1장 | 가로 꽉(화면-32) × 300 |
| 사진 2장 (기본) | 각 (화면-36)/2 × 250, 나란히 |
| 사진 2장 (2번째 세로사진) | 위 가로 꽉 × 220 + 우하단 1/3폭 × 130 |
| 사진 3장 | 위 가로 꽉 × 200 + 아래 2분할 × 140 |
| 수정 모드 슬롯 | 3등분 정사각형 (약 110×110) |
